{
  "name": "kong-assignment",
  "version": "0.0.2",
  "description": "Technical exercise for Kong",
  "homepage": "https://github.com/alecmestroni/kong-assignment#readme",
  "bugs": {
    "url": "https://github.com/alecmestroni/kong-assignment/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alecmestroni/kong-assignment.git"
  },
  "license": "ISC",
  "author": "alec-js",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "cypress run",
    "prepare": "husky install",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down -v",
    "ci": "npm run docker:up && wait-on http-get://localhost:8002 && npm test"
  },
  "dependencies": {
    "cypress": "^15.2.0"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "lint-staged": {
    "*.{js,css,md}": "prettier --write"
  }
}